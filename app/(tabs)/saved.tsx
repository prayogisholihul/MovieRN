import React from 'react'
import { SafeAreaView, Text, View } from 'react-native'

const Saved = () => {
  return (
    <SafeAreaView>
      <Text>saved</Text>
    </SafeAreaView>
  )
}

export default Saved